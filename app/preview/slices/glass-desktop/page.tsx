import { GlassDesktop } from "@/features/glass-desktop";

// Full-bleed preview of the Lucent Desktop slice (the workspace IS the product).
export default function GlassDesktopPreview() {
  return <GlassDesktop />;
}
