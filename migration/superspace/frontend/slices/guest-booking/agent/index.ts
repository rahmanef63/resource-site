/**
 * Guest Booking frontend agent registration stub.
 * Wire to the shared agent registry once AI concierge UI is built.
 */

export function registerGuestBookingAgent() {
  // no-op placeholder; AI tool definitions live in convex/features/guestBooking/agent.ts
}

// Alias for sync-generated registry compatibility.
export const agent = {
  name: "Guest Booking Concierge",
  description: "Hospitality concierge agent (stub).",
  icon: "BedDouble",
}
