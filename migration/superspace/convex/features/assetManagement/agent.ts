import { FeatureAgent } from "../ai/lib/types"

/**
 * Backend Agent for asset-management
 * Provides server-side tools for the feature agent.
 */
export const agent: FeatureAgent = {
    tools: {}
};
