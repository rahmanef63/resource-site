/**
 * Owner Analytics — READ-ONLY feature.
 *
 * Intentionally exports zero `mutation(...)` definitions. This dashboard
 * aggregates existing data from sales/accounting/cashFlowForecast/guestBooking
 * and never writes. File kept for `convex sync` consistency.
 */
export {}
