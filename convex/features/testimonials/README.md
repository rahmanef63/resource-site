# testimonials (convex feature)

CRUD for customer testimonials — the quote cards on marketing public pages,
managed from the admin.

**Tables:** `testimonials`
**Functions:** `query.ts`, `mutation.ts`

Schema composes into the root via `testimonialsTables` in `_schema.ts`.
Part of Rahman Resources.
