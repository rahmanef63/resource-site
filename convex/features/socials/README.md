# socials (convex feature)

Social profile links — the icon row in a template's footer/header, editable
from the admin.

**Tables:** `socialLinks`
**Functions:** `query.ts`, `mutation.ts`

Schema composes into the root via `socialsTables` in `_schema.ts`.
Part of Rahman Resources.
