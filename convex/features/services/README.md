# services (convex feature)

CRUD for a template's "Services" offering — the service cards shown on
marketing/agency public pages and managed from the admin.

**Tables:** `services`
**Functions:** `query.ts`, `mutation.ts`

Schema composes into the root via `servicesTables` in `_schema.ts`.
Part of Rahman Resources.
