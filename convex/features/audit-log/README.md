# audit-log (convex feature)

Append-only operator audit trail — records privileged actions for compliance
and forensics.

**Tables:** `audit_events`

Schema composes into the root via `auditLogTables` in `_schema.ts`.
Part of Rahman Resources.
