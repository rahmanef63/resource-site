# document-checklist (convex feature)

Reusable checklists for documents — instantiate items from named templates and
track completion per document.

**Tables:** `document_checklist_items`, `document_checklist_templates`
**Functions:** `query.ts`, `mutation.ts` · helpers in `_mutation-helpers.ts`

Schema composes into the root via `documentChecklistTables` in `_schema.ts`.
Part of Rahman Resources.
