# admin (convex feature)

Operator-only admin queries (stats, dashboards). Read surface gated behind
`requireAdmin`. No own tables — aggregates across other features.

**Functions:** `query.ts`

Part of Rahman Resources. Install the matching slice via `npx rr add`.
