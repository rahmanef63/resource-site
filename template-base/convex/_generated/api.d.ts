/**
 * TEMPORARY HAND-WRITTEN STUB.
 * `npx convex dev` overwrites this file with real codegen that builds
 * an exact ApiFromModules tree from every file in convex/. We cannot
 * reproduce that here without the deployment — so `api` and `internal`
 * are typed as `any` to unblock `tsc --noEmit` against the studio code.
 *
 * Runtime calls will fail until real codegen runs.
 */

export declare const api: any;
export declare const internal: any;

export declare const components: any;
