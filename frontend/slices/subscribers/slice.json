{
  "$schema": "https://resource.rahmanef.com/slice-schema.json",
  "slug": "subscribers",
  "version": "0.1.0",
  "category": "email",
  "kind": "backend",
  "title": "Subscribers",
  "description": "Newsletter list with public `subscribe` (honeypot + per-email rate-limit + idempotent on email) + token-based `unsubscribe` + admin `remove` + admin queries `listAll` / `count`. Schema includes a `subscriberAttempts` throttle table. Lifted 2026-05-16 from rahmanef.com; sanitized: token-based admin gate replaced with rr's `requireAdmin(ctx)` from `_shared/auth`, dropped sibling `analyticsEvents` insert (wrap in your own action if you want funnel telemetry).",
  "namespace": "@/features/subscribers",
  "frontend": {
    "slicePath": "frontend/slices/subscribers",
    "configExport": "subscribersFeature"
  },
  "convex": {
    "tablesExport": "subscribersTables",
    "schemaPath": "convex/features/subscribers/_schema.ts",
    "rootPaths": ["convex/features/subscribers"]
  },
  "deps": {
    "npm": ["convex@^1.16.0"],
    "shadcn": [],
    "env": [{ "name": "SUPER_ADMIN_EMAIL", "scope": "convex", "required": false, "description": "Used by requireAdmin from _shared/auth — admin queries gate on this." }],
    "peers": [{ "slug": "convex-auth", "range": "^0.1", "reason": "requireAdmin calls getAuthUserId from @convex-dev/auth." }]
  },
  "registers": [],
  "audit": [],
  "license": "MIT",
  "tags": ["email", "newsletter", "subscribers", "convex", "backend"]
}
