"use client";import{useSyncExternalStore,type ComponentType}from"react";import type{Icon}from"@phosphor-icons/react";import{registerCommands}from"../lib/commands";import{getShellMeta,registerShellMeta,resolveShellId,setShell,shellPrefsStore,surfaceOf,type ShellId,type ShellPrefs,type ShellSurface}from"./shell-core";export type{ShellId,ShellPrefs,ShellSurface}from"./shell-core";export{setShell,surfaceOf}from"./shell-core";
export type ShellDescriptor={id:ShellId;label:string;icon:Icon;surface:ShellSurface;group:"Desktop"|"Mobile";windowed?:boolean;wallpaper?:string;render:ComponentType};const REGISTRY=new Map<ShellId,ShellDescriptor>();const order:ShellId[]=["macos","windows","mobile","ios","android"];
export function registerShell(d:ShellDescriptor){REGISTRY.set(d.id,d);registerShellMeta({id:d.id,label:d.label,surface:d.surface,group:d.group,windowed:d.windowed,wallpaper:d.wallpaper});syncShellCommands()}function syncShellCommands(){registerCommands("shells",shellList().map(s=>({id:`shell:${s.id}`,label:`Switch ${s.surface} shell: ${s.label}`,hint:"Shell",keywords:`os layout ${s.id}`,run:()=>setShell(s.surface,s.id)})))}export function getShell(id:ShellId|null|undefined){return id?REGISTRY.get(id):undefined}export function shellList(){return order.map(id=>REGISTRY.get(id)).filter(Boolean) as ShellDescriptor[]}export function useShellPrefs(){return useSyncExternalStore(shellPrefsStore.subscribe,shellPrefsStore.get,shellPrefsStore.defaults)}export function shellsForSurface(surface:ShellSurface){return shellList().filter(s=>s.surface===surface)}export function resolveShell(surface:ShellSurface,p:ShellPrefs){const id=resolveShellId(surface,p);return REGISTRY.get(id)??shellList().find(s=>s.surface===surface)!}
