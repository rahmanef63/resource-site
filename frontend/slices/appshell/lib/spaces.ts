"use client";import{useState,useSyncExternalStore}from"react";import{activeSpaceStore}from"./spaces-core";export*from"./spaces-core";export function useActiveSpace(){return useSyncExternalStore(activeSpaceStore.subscribe,activeSpaceStore.get,activeSpaceStore.get)}export function useSpaceSlideDirection(active:number):"left"|"right"|null{const[prev,setPrev]=useState(active),[dir,setDir]=useState<"left"|"right"|null>(null);if(prev!==active){setDir(active>prev?"right":"left");setPrev(active)}return dir}
