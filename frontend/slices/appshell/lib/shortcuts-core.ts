import { registerCommands } from "./commands-core"; export type ShortcutHint={keys:string;label:string;group?:string};let hints:ShortcutHint[]=[];const sources=new Map<string,ShortcutHint[]>(),subs=new Set<()=>void>();let open=false;const emit=()=>{hints=[...sources.values()].flat();subs.forEach(f=>f())};
export const shortcutStore={subscribe(cb:()=>void){subs.add(cb);return()=>subs.delete(cb)},get:()=>hints,getOpen:()=>open};export function registerShortcuts(source:string,list:ShortcutHint[]){sources.set(source,list);emit();return()=>{if(sources.get(source)===list){sources.delete(source);emit()}}} export const listShortcuts=()=>hints; export function setShortcutHelpOpen(v:boolean){if(open===v)return;open=v;subs.forEach(f=>f())}
registerShortcuts("appshell",[{keys:"⌘K",label:"Command palette",group:"System"},{keys:"⌘I",label:"AI Inspector",group:"System"},{keys:"⌘/",label:"Keyboard shortcuts",group:"System"},{keys:"⌘⇧V",label:"Clipboard history",group:"System"},{keys:"Space",label:"Quick Look (selected item)",group:"System"},{keys:"⌘Tab",label:"App switcher",group:"Windows"},{keys:"F3",label:"Mission Control",group:"Windows"},{keys:"⌘←/→",label:"Snap window left / right",group:"Windows"},{keys:"⌘↑",label:"Maximize window",group:"Windows"},{keys:"⌘↓",label:"Restore / minimize window",group:"Windows"}]);registerCommands("shortcut-help",[{id:"shortcuts:open",label:"Keyboard shortcuts",hint:"Help",keywords:"hotkeys cheat sheet bindings keys",run:()=>setShortcutHelpOpen(true)}]);
