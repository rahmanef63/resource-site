import{M,emit,patch,shellStore,topWindowBy}from"./store-state";import{registerCommands}from"./commands-core";import{focusWindow}from"./store";import{spaceOf}from"./spaces-core";import{toast}from"./toast-core";import type{WinId}from"./types-core";export const groupMembers=(g:string)=>M.state.order.filter(id=>M.state.windows[id]?.groupId===g);export const groupTop=(g:string)=>topWindowBy(groupMembers(g));export function activateTab(id:WinId,frameOf:WinId){const f=M.state.windows[frameOf];if(!f)return;patch(id,{x:f.x,y:f.y,w:f.w,h:f.h,minimized:false});focusWindow(id)}export function mergeFocusedAppWindows(){const fid=M.state.focused,focused=fid?M.state.windows[fid]:undefined;if(!fid||!focused)return 0;const members=M.state.order.filter(id=>{const w=M.state.windows[id];return w&&w.app===focused.app&&spaceOf(w)===spaceOf(focused)});if(members.length<2)return 0;for(const id of members)patch(id,{groupId:fid,x:focused.x,y:focused.y,w:focused.w,h:focused.h,minimized:false});focusWindow(fid);return members.length}export function ungroup(g:string){groupMembers(g).forEach((id,i)=>{const w=M.state.windows[id];if(w)patch(id,{groupId:undefined,x:w.x+i*28,y:w.y+i*28})});emit()}export const windowTabsStore={subscribe:shellStore.subscribe};registerCommands("window-tabs",[{id:"tabs:merge",label:"Merge windows into tabs",hint:"Window",keywords:"group stack combine app",run:()=>{const n=mergeFocusedAppWindows();toast(n>=2?`Merged ${n} windows into tabs`:"Need 2+ windows of the same app")}},{id:"tabs:separate",label:"Separate tabs",hint:"Window",keywords:"ungroup unstack split windows",run:()=>{const fid=M.state.focused,g=fid?M.state.windows[fid]?.groupId:undefined;if(!g)return void toast("Focused window has no tabs");ungroup(g);toast("Separated tabs")}}]);
