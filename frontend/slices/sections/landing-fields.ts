import { LANDING_FIELDS_CORE, type LandingFieldDef } from "./lib/fields";

/** Framework-neutral editor field schema (legacy export name preserved). */
export const LANDING_FIELDS: LandingFieldDef[] = LANDING_FIELDS_CORE;
export type { LandingFieldDef };
