import { defineFeature } from "@/lib/shared/features/defineFeature";

export const servicesFeature = defineFeature({
  slug: "services",
  title: "Services",
  category: "content",
  routes: [],
  nav: { label: "Services", group: "content", order: 71 },
});
