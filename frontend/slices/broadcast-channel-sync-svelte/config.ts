type BroadcastChannelSyncConfig = {
  slug: "broadcast-channel-sync";
  title: string;
  category: "data";
  routes: unknown[];
};

export const broadcastChannelSyncConfig = {
  slug: "broadcast-channel-sync",
  title: "BroadcastChannel — Cross-tab Sync",
  category: "data",
  routes: [],
} satisfies BroadcastChannelSyncConfig;
