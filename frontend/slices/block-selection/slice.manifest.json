{
  "name": "block-selection",
  "description": "Framework-agnostic multi-selection for vertical lists — provider + SelectableBlock wrapper + useBlockSelection. Edge-click select (Shift range / Cmd toggle), Backspace bulk-delete, floating count toolbar.",
  "deps": {
    "shared": ["lib/utils"],
    "slices": [],
    "convex": []
  },
  "notes": "Pure client-side slice — no Convex, no npm deps, no shadcn primitives. Host owns the data and runs onBulkDelete(ids)."
}
