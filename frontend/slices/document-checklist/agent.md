<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `document-checklist` — Document Checklist — Job-Search Doc Tracker

> Track required job-search documents (CV, KTP, ijazah, etc.

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/document-checklist
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=document-checklist
🤖 Prompt page    : https://resource.rahmanef.com/agents/document-checklist

## 1. Install

```bash
npx rahman-resources add document-checklist
# alias: npx rr add document-checklist
```

## 2. What it ships

- tags: `career`, `documents`, `checklist`, `job-search`, `indonesia`

## 3. Wire it up

Run `rr add document-checklist`. Wire <DocumentChecklist bindings={{ current, seed, updateStatus }} countryTemplateSlot={<CountryTemplateCard bindings={{ templates, getTemplate, instantiate }} />} /> — bindings sourced from api.features['document-checklist'].* queries/mutations.

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
