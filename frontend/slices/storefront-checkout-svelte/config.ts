export type StorefrontCheckoutConfig = {
  slug: string;
  title: string;
  category: "content";
};

export const storefrontCheckoutConfig: StorefrontCheckoutConfig = {
  slug: "storefront-checkout",
  title: "Storefront Checkout — guest cart + checkout composition",
  category: "content",
};

export default storefrontCheckoutConfig;
