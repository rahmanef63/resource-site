export type AiRouterConfig = {
  slug: "ai-router";
  title: "AI Router — OpenRouter Tier Proxy";
  category: "ai";
  routes: [];
  nav: { label: string; group: "ai"; order: number };
};

export const aiRouterConfig: AiRouterConfig = {
  slug: "ai-router",
  title: "AI Router — OpenRouter Tier Proxy",
  category: "ai",
  routes: [],
  nav: { label: "AI usage", group: "ai", order: 0 },
};
