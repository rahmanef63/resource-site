<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `equation` — Equation — Notion-style KaTeX block primitive

> Inline-or-display LaTeX equation block — Notion-inspired primitive.

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/equation
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=equation
🤖 Prompt page    : https://resource.rahmanef.com/agents/equation

## 1. Install

```bash
npx rahman-resources add equation
# alias: npx rr add equation
```

## 2. What it ships

- tags: `ui`, `notion`, `notion-like`, `equation`, `katex`, `latex`, `math`, `block`, `primitive`, `editor`

## 3. Wire it up

Run `npx rr add equation`. Single npm dep: katex. Import `import { EquationBlock } from "@/features/equation"`. Props-driven: pass `value` (LaTeX string) + `onChange`. For display-mode (block-level) pass `displayMode`. Renders raw text if KaTeX parse fails. Bundled inside notion-blocks if you also want code/notify/drag-fill.

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
