export type RbacRolesConfig = {
  slug: string;
  title: string;
  category: "auth";
};

export const rbacRolesConfig: RbacRolesConfig = {
  slug: "rbac-roles",
  title: "RBAC — Roles & Permissions",
  category: "auth",
};

export default rbacRolesConfig;
