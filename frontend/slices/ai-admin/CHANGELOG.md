# Changelog — ai-admin

## 0.4.0 — 2026-09-14

- Corrected the slice to match its canonical source: compile-time types/config only.
- Removed nonexistent runtime, storage, component, and synthetic preview claims.
- Added explicit Svelte/SvelteKit metadata that reuses the same framework-neutral TypeScript source.
