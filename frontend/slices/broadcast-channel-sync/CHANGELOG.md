# Changelog — broadcast-channel-sync

## 0.2.0 — 2026-06-10

- Agentic tool collection (`lib/tools.ts`): `broadcastChannelSyncTools` — read/publish over one host-owned useBroadcastSync instance (`BroadcastChannelSyncCtx`).
