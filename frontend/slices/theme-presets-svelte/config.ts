export type ThemePresetsConfig = {
  slug: string;
  title: string;
  category: "ui";
};

export const themePresetsConfig: ThemePresetsConfig = {
  slug: "theme-presets",
  title: "Theme Presets — unified switcher with bundled tweakcn registry",
  category: "ui",
};

export default themePresetsConfig;
