import { defineFeature } from "@/lib/shared/features/defineFeature";

export const loadingStatesFeature = defineFeature({
  slug: "loading-states",
  title: "Loading States — skeletons + spinners SSOT",
  category: "ui",
  routes: [],
  nav: { label: "Loading States", group: "ui", order: 83 },
});
