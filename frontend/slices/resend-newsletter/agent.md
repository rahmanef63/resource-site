<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `resend-newsletter` — Resend — Transactional & Newsletter

> Transactional email + newsletter blast via Resend.

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/resend-newsletter
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=resend-newsletter
🤖 Prompt page    : https://resource.rahmanef.com/agents/resend-newsletter

## 1. Install

```bash
npx rahman-resources add resend-newsletter
# alias: npx rr add resend-newsletter
```

## 2. What it ships

- tags: `email`, `newsletter`, `resend`

## 3. Wire it up

Run `npx rr add resend-newsletter`. Use Resend Audiences API for newsletter — store subscriber emails in Convex too for segmentation. Double opt-in: subscriber.create with status 'pending' → click link → status 'confirmed'.

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
