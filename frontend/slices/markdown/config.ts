/** Framework-neutral feature metadata; self-contained for CLI installs. */
export const markdownFeature = {
  slug: "markdown",
  title: "Markdown — page container with CRUD tabs + diagrams",
  category: "content",
  routes: [],
  nav: { label: "Markdown", group: "content", order: 62 },
} as const;
