<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `convex-auth` — Convex Auth — Multi-Provider Sign-in

> @convex-dev/auth with Password (PBKDF2-SHA256 100k, self-hosted-friendly), Anonymous (guest), Google OAuth, and Resend magic-link.

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/convex-auth
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=convex-auth
🤖 Prompt page    : https://resource.rahmanef.com/agents/convex-auth

## 1. Install

```bash
npx rahman-resources add convex-auth
# alias: npx rr add convex-auth
```

## 2. What it ships

- tags: `auth`, `convex`, `password`, `magic-link`, `google`, `anonymous`, `no-clerk`, `pbkdf2`

## 3. Wire it up

Run `rr add convex-auth`. Then create convex/auth.ts using the kitab pattern (Resend provider). Set env via `npx convex env set` for self-hosted.

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
