export type SiteSetupWizardConfig = {
  slug: string;
  title: string;
  category: "ui";
};

export const siteSetupWizardConfig: SiteSetupWizardConfig = {
  slug: "site-setup-wizard",
  title: "Site Setup Wizard — first-run site setup",
  category: "ui",
};

export default siteSetupWizardConfig;
