# Changelog — notion-database

## 0.18.0 — 2026-06-10

- Agentic tool collection (`lib/tools.ts`): `notionDatabaseTools` — rows/row.add/row.update/row.delete/view.switch/filter/sort over injectable `NotionDatabaseCtx` built from the host page's view state.
