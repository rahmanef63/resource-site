export type CommandMenuConfig = {
  slug: string;
  title: string;
  category: "ui";
};

export const commandMenuConfig: CommandMenuConfig = {
  slug: "command-menu",
  title: "Command Menu",
  category: "ui",
};

export default commandMenuConfig;
