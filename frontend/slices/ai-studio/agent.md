<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `ai-studio` — AI Studio — Generation Canvas

> AI is the primary UI — single big prompt input → live-streaming output → variation grid → version tree.

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/ai-studio
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=ai-studio
🤖 Prompt page    : https://resource.rahmanef.com/agents/ai-studio

## 1. Install

```bash
npx rahman-resources add ai-studio
# alias: npx rr add ai-studio
```

## 2. What it ships

- tags: `ai`, `ai:studio`, `generation`, `streaming`, `history`, `branching`, `image-gen`

## 3. Wire it up

Run `npx rr add ai-studio`. Mount `<GeneratorCanvas />` at /. Use case: prompt → output IS the entire product. Wire your output renderer (text/image/code/audio) via the OutputSlot adapter. Templates loaded from ai-admin.studio.templates.

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
