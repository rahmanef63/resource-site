<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `database-io` — [DEPRECATED] Database IO — merged into notion-database v0.6

> DEPRECATED — all functionality merged into `notion-database` v0.

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/database-io
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=database-io
🤖 Prompt page    : https://resource.rahmanef.com/agents/database-io

## 1. Install

```bash
npx rahman-resources add database-io
# alias: npx rr add database-io
```

## 2. What it ships

- tags: `ui`, `notion`, `notion-like`, `database`, `csv`, `json`, `import`, `export`, `template`, `data`, `backup`, `spreadsheet`, `wire-format`, `primitive`, `optional`

## 3. Wire it up

⚠️ DEPRECATED — use `npx rr add notion-database` (now bundles import/export). This slice is a thin re-export shim from `@/features/notion-database` for back-compat. New code: `import { DatabaseIOActions } from "@/features/notion-database"`. Remove when migration complete; slice scheduled for removal in v1.0.

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
