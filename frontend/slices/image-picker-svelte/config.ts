export type ImagePickerConfig = {
  slug: string;
  title: string;
  category: "ui";
};

export const imagePickerConfig: ImagePickerConfig = {
  slug: "image-picker",
  title: "Image Picker — gallery · upload · link · Unsplash · reposition",
  category: "ui",
};

export default imagePickerConfig;
