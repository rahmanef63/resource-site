export type NotificationsCenterConfig = {
  slug: string;
  title: string;
  category: "ui";
};

export const notificationsCenterConfig: NotificationsCenterConfig = {
  slug: "notifications-center",
  title: "Notifications Center — bell + inbox",
  category: "ui",
};

export default notificationsCenterConfig;
