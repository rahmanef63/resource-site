{
  "name": "notion-blocks",
  "description": "Peer bundle re-exporting equation + code-block + notifications + database-cell-selection.",
  "deps": {
    "shared": [],
    "slices": ["equation", "code-block", "notifications", "database-cell-selection"],
    "convex": []
  },
  "notes": "Pure barrel — no logic of its own. Each peer slice owns its component code + tests + sync registry entry. Drop notion-blocks if downstream wants narrow imports."
}
