<!-- AUTO-GENERATED by scripts/features/gen-slice-agent-md.mjs -->
<!-- DO NOT EDIT — re-run `node scripts/features/gen-slice-agent-md.mjs` -->

# Install `midtrans-payment` — Midtrans — Indonesia Payment

> Pembayaran lokal Indonesia via Midtrans Snap (BCA, Mandiri, BRI, e-wallet GoPay/OVO/Dana, QRIS).

📚 Knowledge base : https://resource.rahmanef.com/llms.txt
📦 Slice detail   : https://resource.rahmanef.com/slices/midtrans-payment
🧠 JSON catalog   : https://resource.rahmanef.com/api/knowledge?slice=midtrans-payment
🤖 Prompt page    : https://resource.rahmanef.com/agents/midtrans-payment

## 1. Install

```bash
npx rahman-resources add midtrans-payment
# alias: npx rr add midtrans-payment
```

## 2. What it ships

- tags: `payment`, `midtrans`, `indonesia`, `qris`, `snap`

## 3. Wire it up

Run `npx rr add midtrans-payment`. Midtrans Snap untuk pembayaran instant. Webhook ke Convex HTTP action /api/midtrans-callback untuk update order status. Ingat: PPN 11% sudah included di amount, jangan double-count.

## Tools (agentic surface)

Function-calling tools this slice provides. Register the exported collection on any `@/shared/agentic` host (e.g. `useAgentTools(<x>Tools, ctx)`) and ONE agent can drive this slice alongside others.

**Agent guidance:** Midtrans payments. create_invoice charges and refund reverses. Both move money, confirm with the user first.

- `midtrans-payment.create_invoice` ⚠ destructive
- `midtrans-payment.status`
- `midtrans-payment.refund` ⚠ destructive

⚠ destructive tools are flagged `dangerous: true` — wire the agent loop's `confirm` event so they need user approval before running.

rr ships the function list above plus a custom instruction (`registry.systemPrompt()`); bring your own model + key to call them (BYOK).

## Rules of engagement

- shadcn-only UI primitives. No raw `<button>` / `<dialog>` / native date or file inputs.
- 200-line hard cap per source file (extract neighbours when over).
- All Convex queries hit an index (`.withIndex(...)`); never bare `.collect()`.
- Public mutations/queries declare `args:` validators + authz.
- Full ruleset: https://resource.rahmanef.com/best-practice
