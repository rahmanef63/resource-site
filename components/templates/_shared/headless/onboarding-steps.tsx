// Graduated to a distributable slice (2026-06-06) — SSOT now lives at
// frontend/slices/site-setup-wizard (`npx rr add site-setup-wizard`).
// Re-exported here so existing headless-surface consumers keep working.
export { StepIdentity, StepBranding, StepContent, StepDone, Field } from "@/features/site-setup-wizard";
