// Graduated to a distributable slice (2026-06-06) — SSOT now lives at
// frontend/slices/onboarding-wizard (`npx rr add onboarding-wizard`).
// Re-exported here so existing headless-surface consumers keep working.
export { StepIdentity, StepBranding, StepContent, StepDone, Field } from "@/features/onboarding-wizard";
