// Canonical re-export for the three-column layout shipped at
// components/previews/three-column/. Consumers import via this path so the
// underlying location can move without a downstream churn.

export {
  ThreeColumnLayoutAdvanced,
} from "@/components/previews/three-column/ThreeColumnLayout";
