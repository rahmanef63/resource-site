# `rahman-shared/lib`

Framework-agnostic utilities. NO React, NO Next, NO Convex imports here. Pure TS.

ESLint enforces:
- No `react`/`react-dom` imports
- No `next/*` imports
- No `@/shared/ui/*` imports

Phase 2 harvest from superspace `frontend/shared/lib/`.
